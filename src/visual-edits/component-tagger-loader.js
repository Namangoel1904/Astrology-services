// Component tagger loader for visual edits
// This is a placeholder loader for the visual edits system

module.exports = function(source) {
  // Return the source code as-is for now
  // This loader can be extended to add visual edit tags to components
  return source;
};
